TELEGRAM_BOT_TOKEN=xxx
ALLOWED_USER_ID=xxx
WHISPER_URL=http://localhost:7860
# WHISPER_LANGUAGE=auto  # "auto" for auto-detect, or language name e.g. "english", "polish"
# Windows: D:\Git\bin\bash.exe | macOS/Linux: /usr/bin/bash
CLAUDE_CODE_GIT_BASH_PATH=/path/to/bash
# TODOIST_TOKEN=xxx (optional, td CLI uses its own config via `td config set token`)
# LOG_LEVEL=1  # 0=DEBUG, 1=INFO (default), 2=WARN, 3=ERROR
